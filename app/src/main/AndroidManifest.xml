<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.qna">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QNA">
        <activity android:name=".ViewAnswerActivity"></activity>
        <activity android:name=".FindPasswordActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".QuestionIndexActivity" />
        <activity android:name=".QuestionResultActivity" />
        <activity android:name=".DailyQuestionActivity" />
        <activity android:name=".CategorySettingActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".IntroActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>